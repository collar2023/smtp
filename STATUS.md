# 当前状态

**smtp.aillm.net** 已部署(v20, 2026-06-16 11:44 UTC),功能完整。

**加密模型**: X25519 ECDH + AES-GCM 256,**真端到端**。
**协议层**: RFC 5321 状态机, 14/14 单测覆盖。
**部署**: Cloudflare Worker + D1 + KV + R2(预留)。

**你和朋友互通密件的全部所需**:
- 一个域名界面 (`https://smtp.aillm.net/`)
- 双方愿意**交换 X25519 公钥** (44 字符 base64)
- 各自在浏览器**生成一次密钥**, **备份一次 JSON**

**朋友们不需要懂**:
- CLI (可以纯浏览器)
- 密码学 (X25519 / AES-GCM 自动)
- 协议 (RFC 5321 / SMTP 在底层, 前端只看到按钮)

**他们只要会**:
1. 打开网址
2. 点 "生成新密钥对"
3. 复制完整公钥发给你
4. 收件箱点开看邮件

**不再迭代**, 不画蛇添足。
